# 开发交接（和武）

> 交接对象：接手本项目的开发者 / 未来的自己
> 项目：把 `EDMOK/blue-fish-archive`（蓝色大肥鱼档案馆）复刻成个人插画收藏站 + 自建图片查看器
> 首次整理：2026-09-14 ｜ **最后更新：2026-09-17**
> 当前状态：**线上可用、功能完整、部署链路实测闭环；已为"图片迁往对象存储 / 国内 CDN"埋好伏笔**

## 文档导航

| 章节 | 内容 |
|---|---|
| 一 | 地址、本地工具、相关文档 |
| 二 | 技术形态与当前功能（先读这节） |
| 三 | 环境与凭据（含本机网络限制） |
| 四 | 常用操作：加图 / 删图 / 验证 |
| 五 | **踩过的坑**（28 条：编号 1–27，含 3.5；全部实测，含索引） |
| 六 | 变更记录（倒序） |
| 七 | 技巧与可复用做法 |
| 八 | 已知遗留 / 待办 |
| 九 | 上游差异清单 |
| 十 | 下一步建议 |

---

## 一、成品与地址

| 项 | 值 |
|---|---|
| 线上站点 | https://blue-fish-archive-2.luoyuan2613.workers.dev/ （Cloudflare Workers，推送即部署） |
| GitHub 仓库 | `git@github.com:luoyuan2613-cmyk/blue-fish-archive-2.git`（**SSH**，Public） |
| 本地目录（Windows） | `D:\【杂物间】\fish-gallery` |
| 本地目录（WSL） | `/mnt/d/【杂物间】/fish-gallery` |
| **本地启动器** | `启动本地预览.bat`（起服务 + 开浏览器）、`创建桌面快捷方式.bat` → 桌面「图片站（W）」 |
| 设计预览图（未采纳） | `D:\【杂物间】\design-preview\`（三版方案） |
| 素材备份 | `D:\【杂物间】\local-assets-backup\` |
| 上游参考项目 | https://github.com/EDMOK/blue-fish-archive |

**站点定位**：收录**他人创作的插画 / 动画贺图**（版权归各自作者），因此来源区与页脚版权声明**不可删**。

**相关文档**：

| 文件 | 用途 |
|---|---|
| `README.md` | 结构、日常操作、推送排错、部署与缓存 |
| `UPLOAD.md` | 操作手册：加/删图、本地使用、新增/重命名主题与分区全流程 |
| `架构演进路线图.md` | 图片迁往对象存储 / 国内 CDN 的三阶段计划、选型对比、迁移手册 |
| 本文件 | 完整历史、全部坑点、技巧、遗留项 |

---

## 二、技术形态与当前功能

**纯静态站：无框架、无构建、无后端。** 页面与交互全部原生 JS 实现。

### 文件职责

| 职责 | 文件 / 目录 |
|---|---|
| 页面结构 + 全部文案 | `index.html` |
| 样式（深海贴纸风；本项目大量规则**追加**在文件末尾） | `styles.css` |
| 交互：瀑布流 / 查看器（缩放·平移·最大化）/ 主题 / 分区 / 看板娘 / 回顶端 | `app.js` |
| 素材（唯一需要手动操作的目录） | `data/<主题>/<分区>/` |
| 缩略图 480px WebP q80 / 原尺寸 WebP q85 | `previews/`、`large/`（脚本生成，产物名带 `主题-分区-` 前缀） |
| 数据清单（每「主题×分区」一份） | `stickers/manifest_<主题>-<分区>.json`，结构 `{ storage, items }` |
| 首页立绘 / logo / 看板娘 | `assets/`、`logo/`、`memes/` |
| 生成管线 | `scripts/make_previews.py`（含孤儿清理）、`scripts/sync_stickers.py` |
| CI | `.github/workflows/sync-stickers.yml`、`validate-stickers.yml` |
| 缓存策略（Cloudflare 约定） | `_headers` |
| 部署忽略清单（**命门，勿删**） | `.assetsignore` |
| 本地工具 | `启动本地预览.bat`、`创建桌面快捷方式.bat`、`create_shortcuts.ps1` |

### 当前功能清单（均已实测）

- **瀑布流**：CSS 多列 masonry；卡片宽高由清单的 `width/height` 预占，加载不抖动；**顺序每次加载/切分区随机**（Fisher-Yates）。
  ⚠️ 两个不许破的前提：**洗牌只在首次加载 / 切分区 / 切主题 / 手动重试时发生**（滚动路径绝不重渲染、不重洗牌）；
  **任何情况下都不要从墙里删卡片**（CSS 多列删一张会重排整墙 → 表现成"滚动时闪图换图"，见坑 26），
  加载失败只标记 `.is-broken`。取图一律走 `getImageUrl()`（内部转义 `#` `?`）。
- **查看器**（同一组件两种来源）：
  - **两种形态**：**卡片态**（默认，点开即是）= 白卡 + 纸胶带 + 透明棋盘格，图片按**原始尺寸**适应窗口居中
    （小的不放大、清晰；大的等比缩小），档案条与操作按钮都在卡片里；
    **沉浸态**（点「最大化」后才进）= 卡片装饰全收掉、图片撑满视口，底部控件浮层 + 默认隐藏。
    判据在 `app.js` 的 `isImmersive()`：`isMaximized || !isAtFit()` —— **绝不能写成"打开即沉浸"**。
  - `list`：从瀑布流点开，有上一张/下一张
  - `single`：首页立绘也能点开（`#hero-art-window`，键盘可达）
  - **缩放**：以原始像素为基准，适应窗口 ↔ 100%（1:1）↔ 400%；滚轮 / 双击 / 键盘 `+ - 0`
    （卡片态下滚轮向上 / 双击 = 进沉浸态；逐格缩放与拖动只发生在沉浸态里）
  - **平移**：放大后按住拖动；边界按图片**实际对齐方式**计算（可达最右/最下缘）
  - **最大化**：收掉白卡、图片铺满视口（不调用 Fullscreen API）；状态行与按钮浮在图上且**恒半透明**
    （唤醒 0.6、悬停最多 0.8，绝不恢复实心）
  - **Esc 逐级退出**：归位缩放 → 退出最大化（回到卡片态）→ 关闭
  - **沉浸态控件自动隐藏**：底部控件默认隐藏，指针进入屏幕底部 170px 带内才显现（0.6）；
    键盘操作后临时显现 1.6s；触屏（`hover: none`）常显
- **主题 × 分区**：主题（`manga` / `whale`）各带独立分区；切主题 → 顶部导航与墙顶分区条一起重建；清单按 `主题/分区` 缓存
- **其它**：看板娘可拖拽（位置存 localStorage）、一键回顶端（右侧中部悬浮，滚过一屏才出现）、导航滚动阴影

### 关键函数索引（便于在代码里直接搜）

| 概念 | 函数 / 常量（`app.js`，末行为 `scripts/`） |
|---|---|
| 取图地址收口（换 CDN 用） | `getImageUrl` · `applyStorageConfig` · `storageConfig` · `joinStorageUrl` |
| 查看器打开（两种来源） | `openLightbox` · `openSingleImage` · `showSticker` · `closeLightbox` |
| 缩放 | `applyZoom` · `setZoom` · `zoomBy` · `zoomToFit` · `zoomToOneToOne` · `toggleFitAndOneToOne` · `ZOOM_MAX_PERCENT` |
| 平移 | `clampPan` · `isPannable` · `isAtFit` |
| 沉浸态与控件自动隐藏 | `isImmersive` · `syncImmersive` · `initChromeAutoHide` · `CHROME_REVEAL_BAND` · `CHROME_SUPPRESS_MS` |
| 最大化 / 翻页 / 随机 | `setMaximized` · `MAXIMIZE_HINT` · `stepLightbox` · `shuffleInPlace` |
| 主题 × 分区 | `THEMES` · `themeConfig` · `getActivePartitions` · `renderPartitionNav` · `loadStickers` · `fetchPartition` |
| 看板娘 / 回顶端 / 导航 | `initMascot` · `initBackToTop` · `initNavScroll` · `initTheme` |
| 生成管线（Python） | `discover_partitions` · `storage_block` · `entry_path` · `manifest_text` · `prune` |

### 数据管线

```
data/<主题>/<分区>/  →  make_previews.py  →  previews/ + large/（并清理孤儿产物）
                     →  sync_stickers.py  →  stickers/manifest_<主题>-<分区>.json
                     →  git push          →  CI（如需）→ Cloudflare 自动部署
```

### 已埋好的迁移伏笔（详见 `架构演进路线图.md`）

- 清单带 `storage` 段：`mode` / `baseUrl` / `previewBaseUrl` / `largeBaseUrl`；**留空 = 相对路径 = 行为不变**
- 前端取图统一走 `getImageUrl(path, kind)`（10 处调用），换 CDN 只改清单里的 baseUrl
- `sync_stickers.py` 支持 `GALLERY_STORAGE_MODE=local|remote`（remote 预留从外部 API 拉文件列表）
- CI 预埋 `vars.GALLERY_*`：在仓库 Variables 填 CDN 域名即可切换，**不改代码、不放密钥**

### 缓存策略（`_headers`）

| 路径 | Cache-Control |
|---|---|
| `/data/*`、`/previews/*`、`/large/*` | `max-age=31536000, immutable`（故**换图必须改文件名**） |
| `/logo/*`、`/assets/*`、`/memes/*` | `max-age=600` |
| `index.html`、`app.js`、清单 | `max-age=0, must-revalidate` |

---
## 三、环境与凭据（重要）

- 本机（WSL）**连不上 `github.com:443`**（HTTPS 被阻断），但 **SSH 22 端口可用**、`api.github.com:443` 可用。
  → 所以 git 操作**必须走 SSH**，绝不要用 HTTPS 推送（会像 `git clone` 一样挂死）。
  已验证：`ssh -T git@github.com` → `Hi luoyuan2613-cmyk!`
- 本机**连不上 `*.workers.dev` / `*.pages.dev`**，验证线上站点时用
  `powershell.exe -NoProfile -Command "Invoke-WebRequest ..."`（Windows 侧网络可通）。
- 提交身份：`luoyuan2613-cmyk <luoyuan2613-cmyk@users.noreply.github.com>`
- Python 3.14 + Pillow 12（`pip install Pillow`）
- 预览图渲染链路：Windows 版 Edge 无头模式（本机无 node/playwright）：
  ```bash
  EDGE="/mnt/c/Program Files (x86)/Microsoft/Edge/Application/msedge.exe"
  "$EDGE" --headless=new --disable-gpu --hide-scrollbars --force-device-scale-factor=1 \
    --window-size=1360,1500 --screenshot="C:\Users\Public\dsh-tmp\out.png" \
    "file:///C:/Users/Public/dsh-tmp/mk/vA.html"
  ```
  注意：无头 Edge **走系统代理，连不上 workers.dev**；要渲染线上页面，必须先把线上文件镜像到本地再用 `file://` 渲染。
- ⚠️ **不要把任何 token / API Key 写进本文件或仓库**。

---

---

## 四、常用操作

### 加图 / 删图（本地三条命令）

```bash
# 1) 图片放进对应分区，例如 data/whale/cos/
python scripts/make_previews.py     # 生成缩略图/大图，清理孤儿产物，并顺手清洗 URL 不安全文件名
python scripts/sync_stickers.py     # 重建该分区的清单
git add -A && git commit -m "加图" && git push
```

⚠️ **Windows PowerShell 5.1 不支持 `&&`**，请逐行执行；或用 cmd / Git Bash。
删图同理：删掉 `data/` 里的原图 → 跑两条脚本 → 提交推送。

**网页方式（零安装）**：打开
`https://github.com/luoyuan2613-cmyk/blue-fish-archive-2/tree/main/data/<主题>/<分区>/`
→ `Add file` → `Upload files` → Commit，1~2 分钟后自动上线。
CI 会自动补缩略图与清单并提交回来（`data/**` 在触发路径内）。

**删图要点**：`data/` 里的原图删掉后，**必须跑 `make_previews.py`** —— 缩略图与大图的清理（孤儿产物）
是它负责的，`sync_stickers.py` 只管清单。另外 `data/`、`previews/`、`large/` 都是
`immutable` 缓存（一年），**换图要改文件名**，否则浏览器与 CDN 仍显示旧图（见坑 3）。

### 本地预览

双击 `启动本地预览.bat`（或桌面「图片站（W）」）→ 起 `http://127.0.0.1:8000/` 并打开浏览器；
`--no-browser` 只起服务、可传端口（如 `8080`）。**别用 `file://` 打开 HTML**（会拦截 `fetch` 清单）。

### 验证手段（本项目一直这么干）

- **无头 Edge + 注入断言钩子**跑真实页面：每次改动先在验证副本上跑一整套断言（当前 20~40 条），全绿才提交
  （注意坑 12 的三个环境陷阱：`scrollTo` 不生效、过渡不推进、测量要等过渡结束）
- **线上核对**：本机 WSL 连不上 `*.workers.dev`，必须用 Windows 侧
  `powershell.exe -NoProfile -Command "Invoke-WebRequest ..."` 抓**原始字节**再用 Python 解码比对
  （直接读 `.Content` 会因编码变乱码导致比对失真，见坑 5）

---

## 五、踩过的坑（共 28 条，按重要性排序，全部已实测）

> **阅读提示**：本节跨越项目多个阶段。**早期条目的正文写于目录重构之前**，
> 其中提到的 `media/` 与单数 `manifest.json` 是当时的路径，现在分别是
> `data/<主题>/<分区>/` 与 `stickers/manifest_<主题>-<分区>.json`（结构也已升级为 `{ storage, items }`）。
> 凡涉及"当前行为"的说明（触发路径、缓存规则、操作步骤）都已按现状更新。

### 坑 1（致命）：Cloudflare 自动部署从未成功 —— 静态资源超 25 MiB
**现象**：线上站点永远停在很久以前的快照：已被删除的 `assets/deepseek_whale.png` 仍返回 200，新上传的 `assets/001.png` 一直 404，但 HTML 文案却是最新的。手动部署能成功，自动构建全红叉。

**定位过程**（可复用的排查套路）：
1. 对比"线上能访问的文件"与"仓库里存在的文件"——发现线上存在的是**仓库里早已删除的文件**，说明线上是旧快照。
2. 取线上文件字节算 MD5，与骨架期提交里的占位图对比 → **MD5 完全一致**，确认快照时间点。
3. 向用户要 Cloudflare 构建日志 → 一眼看到：
   ```
   X [ERROR] Asset too large.
   Cloudflare Workers supports assets with sizes of up to 25 MiB.
   We found a file /opt/buildhome/repo/.git/objects/pack/pack-3fe6e46...
   Ensure all assets in your assets directory "/opt/buildhome/repo" conform ...
   ```
4. 仓库里没有任何 `wrangler.toml/jsonc`，Cloudflare 构建设置是：构建命令 `无`、部署命令 `npx wrangler deploy`、根目录 `/`，
   构建时由 wrangler 交互式生成配置，`"assets": { "directory": "." }` —— **整个仓库根目录当静态资源**，于是 `.git/` 里的 pack 对象（>25 MiB）被算进去。

**修复**：仓库根目录新增 `.assetsignore`（Wrangler 的标准忽略文件，与 `.gitignore` 语法相同）：
```
.git
.github
.assetsignore
*.md
scripts
```
**验证**：推送后 45 秒，`assets/001.png` 200、`assets/001.webp` 200、`deepseek_whale.png` 404（幽灵文件消失），
并逐个验证 `data/<主题>/<分区>/`（含中文名）、`previews/`（含中文名）、`large/`、`logo/`、`memes/`、清单 `stickers/*.json` 全部 200。

**经验**：`npx wrangler deploy` + `assets.directory = "."` 的组合**必须**配 `.assetsignore` 排除 `.git`，
否则 Git 仓库一大就必然部署失败，而且报错出现在最后一步，很容易误判成"网络/权限"问题。

### 坑 2：工作流的路径触发范围比想象中窄
`sync-stickers.yml` 只在 `data/**`、`scripts/*.py`、它自己 yml 变化时触发。
→ **单独推清单（`stickers/*.json`）或只删 `previews/`、`large/` 里的文件不会触发任何 CI**。
→ 补救：Actions 页面手动 `Run workflow`（`workflow_dispatch` 已启用）。
→ 安全做法：永远通过改 `data/<主题>/<分区>/` 增删图，本地跑完两条脚本后**一起**提交。
（机器人自己的提交只改 `previews/`、`large/`、清单，所以不会自我触发，没有死循环风险。）

### 坑 3：HTTP 缓存把 404 "焊死"在浏览器里【已修复】
`_headers` 里 `/assets/*` 等原本是 `public, max-age=604800`（7 天）。
修复部署前浏览器请求立绘拿到 404 并缓存，**修复后仍显示空白**，必须强刷（`Ctrl+Shift+R`）或无痕窗口。
自测技巧：加 `?v=1` 之类的参数绕开缓存。

**已修复（提交 `e73f59d`）**：`/assets/*`、`/logo/*`、`/memes/*` 的 `max-age` 由 604800 改为 **600（10 分钟）**，
理由与后果都写进了 `_headers` 的注释。`data/`、`previews/`、`large/` 保持 `immutable` 不变。
线上实测（逐个核对响应头）：
```
/index.html /styles.css /app.js /stickers/manifest_*.json → public, max-age=0, must-revalidate
/assets/* /logo/* /memes/*                              → public, max-age=600
/data/* /previews/* /large/*                            → public, max-age=31536000, immutable
```
→ 仍然需要**手工强刷一次**：已经被旧策略缓存到访客本地的 404/旧图不会自动失效，`max-age` 只影响之后的请求。

### 坑 3.5：部署切换期站点会短暂不可达
推送后 Cloudflare 替换实例的几十秒里，`workers.dev` 域名的请求会**全部超时**（本机 `Invoke-WebRequest` 报"无法连接到远程服务器"），
而同一时刻百度、cloudflare.com 都正常 —— 别误判成"站点挂了"或"网络坏了"，等 1 分钟再试即可。
实测：改 `_headers` 后约 60 秒恢复，恢复时新缓存头已生效。

### 坑 4：Cloudflare 项目名与域名形态
用户实际拿到的是 **Workers 项目**，域名是 `blue-fish-archive-2.luoyuan2613.workers.dev`，
**不是** `blue-fish-archive-2.pages.dev`（Pages 域名探测一直失败，别浪费时间）。
`_headers` 在 Workers 静态资源下同样生效（实测响应头里有 `Cache-Control: public, max-age=0, must-revalidate`）。

### 坑 5：PowerShell 抓网页的编码陷阱
`Invoke-WebRequest | Out-File -Encoding utf8` 会加 BOM 且把 UTF-8 当 Latin-1 读，导致中文全变乱码、
逐字节 diff 出 131 行假差异。**正确做法**：把原始字节写盘再比对：
```powershell
[System.IO.File]::WriteAllBytes($out, $r.RawContentStream.ToArray())
```
另：`curl.exe` 是 Windows 程序，**写不了 WSL 的 `/tmp`**，输出路径要给 Windows 路径。

### 坑 6：文件名里的空格与"假 WebP"
`assets/02 .webp`（`02` 与扩展名之间有空格）这类名字在 URL 里要编码成 `%20`，极易踩坑（该文件已于 2026-09-15 清除）；
且那个文件和同名 `.jpg` **字节数完全相同（194388）**——它只是改了扩展名的 JPEG，不是真 WebP。
→ 用户已自行删除。提交前建议统一用 ASCII 无空格文件名（图片本身放 `data/<主题>/<分区>/` 时中文名是没问题的）。
→ 判断只信魔数，别信扩展名：`file -b x.webp` 会打印 `RIFF ... Web/P image` 才是真 WebP。

### 坑 7：本地仓库与远端分叉
用户直接在 GitHub 网页上编辑（`Update index.html`、`Add files via upload`），本地仓库会落后十几个提交；
若本地还有未提交的删除/新增，`git merge --ff-only` 会被拒（"Please move or remove them before you merge"）。
处理顺序：**先备份未跟踪文件到仓库外 → 丢弃冗余改动 → 同步**。本次备份在 `D:\【杂物间】\local-assets-backup\`。

### 坑 8：CI 回写权限要显式开启，且"空跑"验证不了
`Settings → Actions → General → Workflow permissions` 必须是 **Read and write**，
否则机器人无法把生成的缩略图/清单推回仓库（Web UI 无法用 API 查，未授权返回 401）。
**验证技巧**：故意制造"必须回写"的场景——往 `data/<主题>/<分区>/` 放一张新图但**不**本地生成缩略图，
推送后 CI 必须生成并 commit。实测成功后提交历史里会出现 `github-actions[bot] chore: sync sticker manifest and previews`。

### 坑 9：上游把 `alt` 硬编码成角色名
`sync_stickers.py` 里原本写死 `"alt": "鲸鱼娘同人表情包"`。
本项目已提取为常量，改这一行即可：
```python
DEFAULT_ALT = "动画贺图收藏"
```

---

### 坑 10：跨主题同名文件导致缩略图互相覆盖
`previews/`、`large/` 早期只按 `path.stem` 命名（`01.webp`）。主题化之后，两个主题各有一个 `01.jpg`，
必然覆盖 → A 主题显示 B 主题的图。**解决**：产物名带 `<主题>-<分区>-` 前缀。
**教训**：任何"按文件名生成中间产物"的管线，在引入第二套同名数据源时都会静默出错，
且页面不报错、只是图不对——比 404 更难发现。

### 坑 11：初始化顺序错误被"后来的功能"掩盖
`initTheme()` 里原本是 `applyTheme()` 先、`initThemeTabs()` 后。`applyTheme` 内部会遍历
`.theme-tab` 标记选中态，而那时标签还没建出来 → **页面主题正确、但没有任何书签头是高亮状态**。
上一版把分区逻辑排在主题之后，恰好掩盖了它；这版重构才暴露。
**教训**：凡是"A 函数操作 B 函数创建的 DOM"，初始化顺序必须显式写清并加注释；
这类 bug 不会抛错，只会"看起来正常"。

### 坑 12：无头浏览器验证的三个陷阱（会让人误判代码有 bug）
用 Edge `--headless=new` + `--virtual-time-budget` 做自动化验收时，以下三件事**在虚拟时间下不工作**，
表现为"代码明明对、断言却失败"：

| 陷阱 | 现象 | 对策 |
|---|---|---|
| `window.scrollTo()` | 调用后 `scrollY` 恒为 0 | 改用 `document.scrollingElement.scrollTop = N` 制造滚动状态；并用 spy 替换 `scrollTo` 验证调用 |
| `html { scroll-behavior: smooth }` | `scrollTop` 赋值变成动画，读到中间值（实测 419） | 测试期间临时 `documentElement.style.scrollBehavior = 'auto'` |
| CSS `transition` | 类生效后 `computed opacity` 仍停在 0 | 测试期间临时 `el.style.transition = 'none'` 再读计算样式 |

另：**Edge 命令会被转交给已存在的实例**，此时 `--user-data-dir` / `--screenshot` 会被忽略（表现为截图内容不变、
进程数不增加）。每次验证都用一个新的 profile 目录，并确认没有并发调用。

### 坑 13：删图不会清理旧产物（已修）
`make_previews.py` 原先只生成、不删除，所以删掉一张图后 `previews/`、`large/` 里的旧文件会永久残留
（还会被 CI 一起提交、一起部署）。现已加入孤儿清理（默认开启，`--no-prune` 可关），
只清理"前缀仍被发现"的分区，避免因为目录暂时缺失误删整批产物。








### 坑 14：CSS 优先级把功能"压住"，表现成"点了没反应"
**现象**：先点「最大化」，再滚轮或点 ＋− **完全没变化**，只有翻页按钮在动。

**定位**（写了个探针把每步尺寸打出来，数据说话）：

| 阶段 | 读数 | 图片实际矩形 | 可视框 |
|---|---|---|---|
| 最大化后（适应） | 适应 | 521×727 | 521×727 |
| 最大化后点 ＋ | 68% | **537×727** | 537×727 |
| 最大化后滚轮 | 106% | **833×733** | 833×733 |
| 最大化后连点到上限 | 400% | **1325×741** | — |

看高度那一列：无论 68% 还是 400%，高度都死死卡在 **727px = 92vh**。
根因：`lightbox.is-maximized .lightbox-media-shell img { max-height: 92vh }` 是
**3 个类 + 元素**，而缩放的 `lightbox-media-shell.is-zoomed img { max-height: none }` 只有
**2 个类 + 元素** —— 优先级更低，被压住，图片永远被压回原框。

**教训**：JS 逻辑正确、状态类也加上了、读数也在变，但"看不出变化"——当"看起来没反应"时，
优先怀疑**样式是否真的生效**（比对选择器优先级），而不是继续查 JS。
**修法**：用同等或更高优先级的选择器覆盖（本项目用
`.lightbox.is-maximized .lightbox-media-shell.is-zoomed img` 双写）。

### 坑 15：`transform` 会让 fixed 子元素跟着一起缩放（测量假象）
面板有 `transform: scale(0.97)` 的过渡；`transform` 会成为 `position: fixed` 后代的**包含块**，
于是"固定宽度"的按钮在过渡期间被等比缩放到 `460 × 0.97 ≈ 447`。
→ 断言宽度类指标时，必须**等过渡结束**（本项目等 700ms），否则量到的是中间态。

### 坑 16：CSS 网格的"居中"对超大子项会退化成左上对齐 → 平移范围算错一半
**现象**：图片放大后"往左拖不动、往上拖看不到下半身"，只能往右/往下拖。

**取证**（探针实测，图片 3307×2335，可视框 1354×741）：

| 指标 | 实测 |
|---|---|
| `图片左边 − 可视框左边` | **0**（左对齐，不是居中） |
| `图片上边 − 可视框上边` | **0**（上对齐） |
| 代码假设的可平移范围 | ±977 / ±797 |
| 实际需要的范围 | **0 ~ −1953 / 0 ~ −1594** |

**根因**：`.lightbox-media-shell` 用了 `place-items: center`，但当**子项比容器大**时，
CSS 规范要求退化成 start 对齐（否则内容会被永久裁掉）。而 `clampPan()` 却按"居中"算
`±(溢出量 / 2)`，于是有一半画面永远拖不到。

**修法**：不要假设对齐方式，用**布局值**推算——`img.offsetLeft/offsetTop`
（不受 transform 影响）作为锚点，分两种情况给区间：
- 图片比框大：`[-溢出量 − anchor, −anchor]`（两端都要能顶到框边）
- 图片比框小：固定在 `(框 − 图) / 2 − anchor`（居中）
并给 `.lightbox-media-shell` 加 `position: relative`，让 `offsetLeft/Top` 相对可视框计算。

**教训**：凡是"我算的居中"和"浏览器实际摆的"可能不一致的地方（超大子项、flex/grid 的 safe 对齐、
滚动条占位），都要用**实测值**而不是公式假设。

### 坑 17：只缩放（未最大化）时，控件会被 92vh 的图片挤出视口
沉浸态原先只绑定在 `is-maximized` 上，于是"只缩放不最大化"时底部控件仍在文档流里，
被 92vh 的图片推到屏幕外，表现为"按钮几乎不可见"。
→ 修法：把浮动规则从 `.lightbox.is-maximized` 改为 `.lightbox.is-immersive`（最大化 **或** 已缩放），
并整体上移（操作条距底 30px、缩放条 84px、状态行 132px），透明度加重到 0.22。

### 坑 18：`:hover` 把"半透明"顶掉 → 表现成"点了按钮却没变淡"
**现象**：点「最大化」或点缩放条上的按钮后，底部控件看起来没变淡；而用**滚轮/双击**放大就会变淡。

**根因**：那版规则是"沉浸态 opacity 0.22 + `:hover` 恢复实心"。
点按钮时**鼠标仍停在按钮上** → `:hover` 一直成立 → 半透明被顶成实心；
而滚轮/双击时鼠标在图片上 → 不触发 hover → 半透明正常生效。

**教训**：凡"靠 hover 反转状态"的可见性设计，都要想清楚**操作后指针还在原地**这种情况；
用鼠标点完按钮的场景，hover 往往比你想的更"粘"。现在的做法是**默认隐藏 + 指针靠近才显现**（见变更记录）。

### 坑 19：hover 门控的可见性在触屏上等于"永远不可见"
查全项目发现：**没有任何 `@media (hover: none)` 规则**。
触屏没有 hover 事件，"靠近才显现"这类设计会让按钮在手机上永远出不来。
→ 修法：给沉浸态控件加 `@media (hover: none)` 分支：触屏上常显（略淡 0.92）。
**排查同类问题的动作**：搜 `:hover` 且 `hover` 规则里带 `opacity/visibility/display` 的选择器，
逐个问"没有 hover 的设备怎么办"。

### 坑 20：`transform` 过渡期间，`position: fixed` 的后代会"错位"（坑 15 的真实后果）
**现象**：点「最大化」进入缩放状态的那一瞬间，底部按钮**错位**（位置跳动、跑到图片框底部而不是屏幕底部）。

**根因**：`transform`（**哪怕只是过渡中的单位矩阵**）会让元素成为 `position: fixed` 后代的**包含块**。
面板有 `transition: transform 500ms`，进入沉浸态时过渡期间的 transform 存在
→ 控件的 `bottom: 30px` 变成"相对图片框"而不是"相对视口" → 错位。
（坑 15 当时只被当作"测量假象"，其实它是**真实可见的 bug**。）

**修法**：沉浸态把面板的 transform **连同过渡一起**取消
（`.lightbox.is-immersive .lightbox-panel { transform: none; transition: none; }`）。
**断言写法**：用"距视口底部多少像素"来测（`innerHeight - rect.bottom`），
修复前实测 50px / 104px，修复后 30px / 84px、状态行 132px——这条断言能精确抓住该 bug。

### 坑 21：清理"重复规则"时删过头（因为它同时设了定位与偏移）
删掉 `.lightbox.is-maximized .action-status { position: fixed; bottom: 68px; ... }` 时，
我误以为它被 `is-immersive` 版本完全取代——其实 immersive 版本只覆盖了 `bottom`，
**`position: fixed` 正是那条规则提供的**，删掉后状态行退回文档流，"距底 132px"失效。

**教训**：判断两条规则是否重复，要**逐属性比对**，而不是看选择器像不像；
删之前把"被删规则提供的属性集合"与"保留规则提供的属性集合"做差集。

### 坑 22：用 `transform` 做显隐动画，会让几何断言随显隐漂移
隐藏态原本写了 `transform: translateY(14px)`（下滑淡出）。但 `getBoundingClientRect` **包含 transform**，
于是"距底 30px"在隐藏时量到 16px（30 − 14）——同一个元素、两种状态两个数，断言没法稳定。
**修法**：显隐只用 `opacity`（+ `pointer-events`），不用位移；需要动画就动 `opacity`。
### 坑 23：焦点式显隐 = 粘滞显隐（"缩放后按钮不隐藏"的真凶）
**现象**：缩放或最大化后，底部「操作条」那一行**一直亮着**，怎么都不消失。

**根因**：那行的可见性挂在 `:focus-within` 上，而**鼠标点过的按钮会一直保持聚焦**
（`app.js` 打开查看器时也主动 `focus()` 了「最大化」按钮）⇒ 条件永远成立。
指纹：只有焦点所在那一行亮，其余三行仍是隐藏（实测 `opActions=0.9 / opZoom=opCaption=opStatus=0`，
一失焦立刻全 0）。

**修法**：干脆不用焦点——显隐只由鼠标决定（动一下出现、停 1.5 秒消失、点按钮立刻消失）。

**教训**：合成 click（`dispatchEvent(new MouseEvent('click'))`）**不会移动焦点**，所以
上一轮用它的断言套件对这条路径**永远是绿的**；凡是与焦点/键盘相关的交互，
合成事件靠不住，改用真实鼠标试或干脆别把显隐挂在焦点条件上。

### 坑 24（致命）：百分比高度在 grid/flex 里不可靠 —— 面板被图片撑开，一次触发三个 bug
**现象**（三个同时出现，看起来像三个独立问题）：
1. 打开图片就超出屏幕，既没缩放居中、也看不到全图；
2. 按住图片拖不动（平移完全失效）；
3. 翻页键跑到屏幕下方，不在垂直中间。

**根因（同一个）**：`.lightbox-panel` 是 `.lightbox`（`display: grid; place-items: center`）
的 grid item，写的是 `height: 100%`——百分比高度要相对**grid 区域**解析，而那块区域是 auto
行轨、又反过来由面板内容决定 → 循环 → 浏览器按 `auto` 处理 ⇒ **面板高度 = 图片原始高度**。
连锁反应正好对应上面三条：
- `img { max-height: 100% }` 同样失去参照 → 图片按原始尺寸渲染 → **超屏**（现象 1）；
- 面板比视口还高 → 翻页键的 `top: 50%` 落在视口下方（现象 3）；
- 可视框跟着图片一起长大 → `isPannable()`（图片是否比可视框大）**恒为 false**
  → `pointerdown` 直接 return → **拖不动**（现象 2）。

**修法**（最终形态：查看器拆成「卡片态 / 沉浸态」两套布局，详见下一节变更记录）：
- **卡片态（默认打开）**：面板就是**内容尺寸**的白卡（`position: relative`，`max-width/max-height`
  用视口单位兜底），可视框贴着图片，图片按原始尺寸显示、超出屏幕才等比缩小
  —— 上限写成 `max-height: calc(100vh - 260px)` 这种**视口单位**，完全不碰百分比高度；
- **沉浸态（点「最大化」后）**：面板才改成 `position: absolute; inset: 0` + `flex-direction: column`，
  可视框 `flex: 1 1 auto; min-height: 0` 吃掉整个舞台，图片再切成绝对定位铺满可视框；
- 翻页键挂在卡片上：卡片永远比视口矮（有 `max-height` 兜底），`top: 50%` 就不会跑出屏幕。

**教训**：凡是"父级高度靠内容撑、子级又用 `height/max-height: 100%`"的链条，在 grid/flex 里
都要当成**不可靠**——百分比只在父级高度"确定"时才生效，而"由内容决定的高度"不是确定值。
要适配视口，就用**视口单位**（`calc(100vh - N)`）或 flex 分配，别用百分比高度。

### 坑 25：flex 项默认会被压缩 —— 放大后的图片被缩回可视框宽度（表现：放大不动、还是拖不动）
**现象**：滚轮/双击放大后，图片涨到一定大小就不动了；而且**怎么都拖不动**。

**根因**：可视框是 `display: flex`，图片是 flex 项，**flex 项默认 `flex-shrink: 1`**，
装不下时浏览器把图片**缩回可视框宽度**；高度 `auto` 又跟着比例一起缩 ⇒ 图片永远不溢出
⇒ `isPannable()` 恒为 false ⇒ 拖拽入口直接 return。
指纹：给图片写死的像素宽度（如 2000px）与实测渲染宽度不一致，实测恒等于可视框宽度。

**修法**：放大态给图片加 `flex: none`（不收缩），图片才会真的超出可视框、被 shell 裁掉，
`offsetLeft/Top` 也就成了 `clampPan()` 需要的锚点。

**教训**：可视框早先是 grid 布局（grid 项不会被这样压缩），改成 flex 居中后**静默**引入了这个
收缩行为——"把容器换成 flex"不是纯视觉改动，它会改变子项尺寸算法。反过来，这也解释了
为什么坑 16 当年量到的平移范围和现在不一样。

### 坑 26（隐蔽）：文件名里的 `#` → 404 → 删卡片 → CSS 多列整墙重排（"滚动时闪一下换成另一张图"）
**现象**：某个分区往下滑、还没滑到底时，**已经显示在屏幕上的图片会概率性"闪一下"，然后变成另一张图**；
慢速滑动也会出现；**等全部加载完就正常了**。

**排查过程（三个方向逐一否定，最后靠线上实测定位）**：
1. 先否定"滚动时重新渲染/重洗牌"：`shuffleInPlace` 只在 `loadStickers → renderStickers` 里跑，
   而 `loadStickers` 的调用点只有 4 个（首次加载、切分区、切主题、手动重试），**滚动路径根本不碰渲染**；
   也没有"滚动加载更多"的逻辑 —— 331 张卡片一次性全渲染，靠 `loading="lazy"` 交给浏览器懒加载。
2. 再否定"预留宽高不对导致图片加载后重排"：用脚本把 390 条清单的 `width/height` 与
   `previews/`、`large/` 里**真实文件的尺寸**逐条比对，比例**零误差**。
3. 于是只剩"图片内容被换掉"这一种可能 → `img.src` 只会在 `onerror` 里被改 → 一定是**加载失败**。

**根因**：素材是从推特存下来的，`data/manga/default/` 里有 **27 个文件名带 `#`**（`…#白聖女と黒牧師 …`）。
而 **`#` 在 URL 里是锚点**，浏览器请求时会把 `#` 之后整段丢掉 → 请求到一个不存在的短路径 → 404。
线上实测（Windows 侧探针）：
```
正确编码 %23 的 URL → HTTP 200      ← 文件确实在服务器上
今天实际请求的截断路径 → HTTP 404
```
404 → `onerror` 换成原图重试（原图同样带 `#` → 也 404）→ 第二次失败执行 **`card.remove()`**。
而墙是 `columns: 4 210px` 的 **CSS 多列（均衡分列）**：从中间抽掉一张卡，**整面墙重新分列**，
屏幕上同一位置的卡片就换成了别的图。链路上的每一步都精确对应用户描述：
只在没加载完时出现（懒加载滚到才 404）、概率性（取决于滚到哪张坏卡）、
**与滑动速度无关**（位置触发，不是竞态）、滑到底就好（27 张坏卡全删完，之后 DOM 不再变化）。

**顺带**：这 27 张图其实**一直没在墙上显示过**（卡片被删了）—— manga/default 实际只有 304 张在墙上，
清单却是 331 条；灯箱大图与「下载原图」对这批图同样是 404。

**修法（两层）**：
1. **前端转义（取图收口处，治本）**：`app.js` 的 `joinStorageUrl()` 里加 `encodeImagePath()` ——
   `encodeURI(path)` 之后再把 `#`→`%23`、`?`→`%3F`（`encodeURI` 认为这俩"合法"，必须手工替换）。
   一处改动同时修好瀑布流缩略图、灯箱大图、下载原图、邻居预取四条路径；完整地址（`http:`、`//cdn`）
   原样返回，避免二次编码。
2. **兜底不再删卡片**：`onerror` 改成只给卡片加 `.is-broken`（图片不显形 + 一句"图片未加载"提示），
   **节点与占位高度全部保留** —— 任何一张图加载失败都不再动布局。
3. **入库防呆**：`sync_stickers.py` 新增 `sanitize_filename()` / `sanitize_source_names()`，
   入库时把 `#` `?` `%` 自动换成 `_`（撞名自动编号，绝不覆盖）；`make_previews.py` 复用同一实现
   并在**生成缩略图之前**先洗名字（顺序反了会先按旧名产出一批 webp）。
   `--check`（PR 只读校验）与 `--no-sanitize`、`GALLERY_SANITIZE_NAMES=0` 都会跳过改名。
   只替换真的会破坏 URL 的字符，**空格与中文保留**（`encodeURI` 能正确处理，改名会把 390 个文件全冲一遍）。

**教训**：文件名是 URL 的一部分，`#` `?` `%` 在路径里必须转义后才发请求；
另外，"从 CSS 多列/瀑布流里删掉一个元素"是**布局级**操作，会重排整墙 ——
任何"清理失败卡片"的想法都要先确认不会动布局。

### 坑 27：`argparse` 的 help 里出现单个 `%` → 脚本连启动都进不去（本次把 CI 搞红了）
**现象**：给两个脚本加了 `--no-sanitize`，help 文案写成 `URL-unsafe characters (# ? %)`，推上线后
CI 的 **Generate previews 步骤直接失败**；本地照样复现：
```
ValueError: badly formed help string
  File ".../argparse.py", line 1761, in _check_help
```

**根因**：`argparse` 会拿 help 文案去做 printf 式格式化（为了支持 `%(default)s` 这类占位），
所以文案里**单独的 `%` 会被当成格式符**，在**解析阶段**就抛异常 —— 连 `--help` 都跑不到，
脚本一启动就死。

**修法**：help 里的 `%` 写成 `%%`（`help="... (#, ?, %%)."`）。

**教训**：**新增 CLI 参数后必须真的把脚本跑一次**（不是只看语法过不过）——
`py_compile` 只证明语法没错，证明不了 argparse 能起来。
这次用到的复现手法值得复用：**把仓库（排除 `.git`）拷到 `/tmp` 里跑**，
既能拿到真实报错，又不会动到真仓库里的 27 个文件名。

### 坑点索引

- 坑 1：Cloudflare 自动部署从未成功 —— 静态资源超 25 MiB
- 坑 2：工作流的路径触发范围比想象中窄
- 坑 3：HTTP 缓存把 404 "焊死"在浏览器里【已修复】
- 坑 3.5：部署切换期站点会短暂不可达
- 坑 4：Cloudflare 项目名与域名形态
- 坑 5：PowerShell 抓网页的编码陷阱
- 坑 6：文件名里的空格与"假 WebP"
- 坑 7：本地仓库与远端分叉
- 坑 8：CI 回写权限要显式开启，且"空跑"验证不了
- 坑 9：上游把 `alt` 硬编码成角色名
- 坑 10：跨主题同名文件导致缩略图互相覆盖
- 坑 11：初始化顺序错误被"后来的功能"掩盖
- 坑 12：无头浏览器验证的三个陷阱
- 坑 13：删图不会清理旧产物
- 坑 14：CSS 优先级把功能"压住"，表现成"点了没反应"
- 坑 15：`transform` 会让 fixed 子元素跟着一起缩放
- 坑 16：CSS 网格的"居中"对超大子项会退化成左上对齐 → 平移范围算错一半
- 坑 17：只缩放时，控件会被 92vh 的图片挤出视口
- 坑 18：`:hover` 把"半透明"顶掉 → 表现成"点了按钮却没变淡"
- 坑 19：hover 门控的可见性在触屏上等于"永远不可见"
- 坑 20：`transform` 过渡期间，`position: fixed` 的后代会"错位"
- 坑 21：清理"重复规则"时删过头
- 坑 22：用 `transform` 做显隐动画，会让几何断言随显隐漂移
- 坑 23：焦点式显隐 = 粘滞显隐（"缩放后按钮不隐藏"的真凶；合成 click 测不出来）
- 坑 24（致命）：百分比高度在 grid/flex 里不可靠 —— 面板被图片撑开，一次触发「超屏 + 拖不动 + 翻页键跑位」三个 bug
- 坑 25：flex 项默认被压缩 —— 放大后的图片被缩回可视框宽度（放大不动、拖不动）
- 坑 26（隐蔽）：文件名里的 `#` → 404 → 删卡片 → CSS 多列整墙重排（"滚动时闪一下换成另一张图"）
- 坑 27：`argparse` 的 help 里出现单个 `%` → 脚本连启动都进不去（把 CI 搞红了）

---

## 六、变更记录（倒序）

### 变更记录（2026-09-15）· 修"滚动时闪图换图"：文件名 `#` 未转义 + 删卡片触发整墙重排
按反馈修"分区往下滑、还没到底时，屏幕上已显示的图片会闪一下变成另一张；加载完就好了"（根因见坑 26）：
1. **`app.js` 取图收口加转义**：新增 `encodeImagePath()`，`joinStorageUrl()` 内对路径做
   `encodeURI` + `#`→`%23`、`?`→`%3F`（完整地址原样返回，避免二次编码）。
   一处修好瀑布流缩略图 / 灯箱大图 / 下载原图 / 邻居预取四条路径。
   线上实测：带 `#` 的图编码后 **200**、原来的截断路径 **404**。
2. **加载失败不再 `card.remove()`**：改为标记 `.is-broken`（CSS 追加：保留占位高度 + 一句淡提示），
   **节点与布局完全不动** —— 任何加载失败都不会再触发 CSS 多列重排（这正是"闪图"的放大器）。
3. **入库防呆**：`sync_stickers.py` 加 `sanitize_filename()` / `sanitize_source_names()`
   （`#` `?` `%` → `_`，撞名自动编号；`--check`、`--no-sanitize`、`GALLERY_SANITIZE_NAMES=0` 可跳过），
   `make_previews.py` 复用同一实现、且在生成缩略图**之前**执行。
   现有 27 个带 `#` 的老文件**不强制改名**（前端已能正常取到），下次跑管线时会自动清洗并更新清单。
4. ⚠️ 首版把 CI 搞红了：`--no-sanitize` 的 help 文案里写了单个 `%`，argparse 解析阶段直接抛
   `ValueError: badly formed help string` → 已改成 `%%`（详见坑 27）。
   修复后在 `/tmp` 的仓库副本里把管线完整跑了一遍：27 个文件改名、52 个产物重生成、清单零 `#`。
5. ⚠️ **CI 回写清单里必须包含 `data/`**：工作流原来只 `git add stickers previews large`，
   而防呆规则会在 CI 里把 `data/` 的源文件改名 —— 结果清单里的 `original` 指向了没被提交的路径，
   表现是「下载原图」和动图原文件全 404（只提交缩略图/大图是不够的）。已改成
   `git add stickers previews large data`，并把 `data` 加进"是否已同步"的判断。
6. 核对确认**洗牌行为无需改动**：`shuffleInPlace` 只在首次加载 / 切分区 / 切主题 / 手动重试时执行，
   滚动路径完全不碰渲染（这是本次排查第一个被排除的怀疑方向，已写进坑 26 存档）。

### 变更记录（2026-09-15）· 查看器拆成「卡片态 / 沉浸态」，控件一律半透明
按反馈调逻辑（上一轮把打开即沉浸态做过头了：点开就是大图，白卡和留白全没了）：
1. **打开即卡片态（图三那种）**：`isImmersive()` 原先是 `lightbox.classList.contains('is-open')`
   —— **只要打开就算沉浸态**，所以点开就没了白卡。现在改成
   `isMaximized || !isAtFit()`：**只有点了「最大化」才沉浸**。
   卡片态 = 白卡 + 纸胶带 + 透明棋盘格 + 图片按原始尺寸适应窗口 + 档案条与按钮都在卡片里。
2. **沉浸态（点「最大化」）**：面板才切成 `position: absolute; inset: 0` 铺满视口、图片撑满屏幕。
   两态差别现在非常明显（卡片 ~340×480 ↔ 满屏 ~900 高），不再"只变大一点点"。
   切换时**必须重测「适应窗口」百分比**（`setMaximized()` 里：先切布局类 → 再量 → 再归位缩放），
   否则滚轮第一格会突然跳变。
3. **卡片态下的"放大" = 进沉浸态**：卡片里图片本来就完整可见，滚轮向上 / 双击 = 点「最大化」
   （`zoomBy` / `toggleFitAndOneToOne` 里各加一个分支）；真正逐格缩放、拖动平移都在沉浸态里做。
4. **控件一律半透明，悬停也不许变实心**（原来唤醒后是 0.9，看着就是实心、挡住图）：
   唤醒态 `opacity: 0.6`；`.lightbox-actions:hover` 只到 **0.8**、档案条 0.78；
   关闭键/翻页键 0.55、悬停 0.85（**原来是 1 = 完全实心**）；触屏常显 0.72。
   原则：这些控件浮在图片上，**绝不能挡住底下的画面**（延续坑 18 的教训）。
5. 翻页键移回卡片内（卡片态贴着卡片两侧，和图上位置一致；卡片有 max-height 兜底，
   不会再出现坑 24 那种"跑到屏幕下方"）。

### 变更记录（2026-09-15）· 修查看器三个体验问题（超屏 / 拖不动 / 翻页键跑位）
> ⚠️ 本条的**第 1、3、4 点已被上一条（卡片态/沉浸态拆分）取代**：那时把整个查看器都做成了
> "铺满视口"，导致点开即大图。现在只有**沉浸态**才是那样，卡片态另有布局。
> 仍然有效的是第 2 点（`flex: none` + `draggable="false"`）和坑 24/25 的结论。

按反馈修"打开图片超屏、按住拖不动、两侧切图键跑到屏幕下方"：
1. **图片自适应（超屏）**：面板改为 `position: absolute; inset: 0` + `flex-direction: column`，
   可视框 `flex: 1 1 auto; min-height: 0`；图片在「适应窗口」态用
   `position: absolute; inset: 0; width/height: 100%; object-fit: contain` 铺在可视框里
   ——不依赖任何百分比高度，永远居中、绝不超屏（根因见坑 24）。
2. **恢复拖拽**：真正的杀手是 **flex 项默认 `flex-shrink: 1`**，把放大后的图片又缩回可视框宽度，
   于是 `isPannable()` 恒为 false、`pointerdown` 直接 return（根因见坑 25）→ 放大态加 `flex: none`；
   另给 `#lightbox-image` 加 `draggable="false"`（避免浏览器原生拖图抢走 pointermove）。
3. **翻页键固定在屏幕垂直正中**：两个按钮从 `.lightbox-panel` 里**拿出来**，挂到 `.lightbox`
   （`position: fixed; inset: 0`）下，`top: 50%` 从此只相对视口，与图片尺寸/面板 transform 无关。
4. `app.js`：`measureFitPercent()` 改为按 contain 定义自算（`min(框宽/原宽, 框高/原高)`）——
   图片改成绝对定位铺满可视框后，`img.clientWidth` 拿到的是可视框宽度、不再是图片显示宽度；
   档案条提示随状态切换（可平移时显示「拖动平移 · 滚轮缩放」）；新增 window resize 重新量「适应」比例。

**顺带发现**：全站 390 张图**最大只有 500px**（最小 182×480），所以 `object-fit: contain` 会把
每一张都放大约 1.2–2.7 倍（中位 1.77×）。若嫌偏软，把该条改成 `scale-down`
（= contain 但绝不放大）即可，同时把 `measureFitPercent()` 里的 `Math.min(boxW/nw, boxH/nh)`
改成 `Math.min(1, ...)` 保持一致。

### 变更记录（2026-09-15）· 查看器改版：删掉缩放按钮行 + 图片居中铺满
- **删掉「− 适应 ＋」那一行**（HTML 节点 + 全部相关 CSS + 监听器一并清除）。
  ⚠️ 缩放能力**不变**：滚轮缩放、双击「适应 ↔ 100%」、键盘 `+ - 0` 全部保留。
- **图片居中 + 铺满**：舞台（`.lightbox-panel`）铺满视口并用 flex 居中，可视框
  （`.lightbox-media-shell`）撑满舞台，图片 `max-width/height: 100%` + `object-fit: contain`
  —— 等比放大到刚好放得下，**不拉伸、不裁剪**，纯 CSS，没有 JS 算尺寸。
  打开即最大；「最大化」= 面板四周边距归零、图片真正贴边；关闭键/翻页键移到 14px 安全区。
- 查看器打开即 `is-immersive`（全屏态），底部控件仍遵循「鼠标一动出现、停 1.5 秒隐藏、点按钮立刻隐藏」。
- 档案条（图片名/尺寸）改为顶部居中浮层，不再占文档流。

### 变更记录（2026-09-15）· 沉浸态控件改为「纯鼠标」控制
- **显隐只看鼠标**：沉浸态（最大化 / 已缩放）下鼠标一动就显现，**停住 1.5 秒自动隐藏**；
  点「最大化」/缩放/下载按钮**立刻隐藏**，画面保持干净。
  实现只有三个函数：`CHROME_IDLE_MS` / `showChromeBriefly()` / `hideChrome()`。
- **彻底删掉焦点/键盘那套**：`styles.css` 不再有 `:focus-within`、也不再有 `:has(:focus-visible)`，
  底栏显隐只剩 `.lightbox.is-immersive.is-chrome-shown` 一条路径；`app.js` 打开查看器也不再
  `focus()` 任何控件（键盘 `+ - 0` 仍能缩放，只是不再用键盘决定控件显隐）。
- **根因**（实测复现）：鼠标点过的按钮会一直保持聚焦 ⇒ 焦点条件永远成立 ⇒ 那一行永远亮着。
  指纹很好认：只有焦点所在那一行亮（`opActions=0.9`），其余三行仍是 0；一失焦立刻全 0。
- 顺带去掉沉浸态「关闭键/翻页键」的 `:focus-visible` 提亮（同一原则：只认鼠标）。
- ⚠️ 教训：上一轮断言套件用的是**合成 click**（不会移动焦点），所以这条路径**永远是绿的** ——
  合成事件测不出真实点击的焦点行为，别再用它验证"跟焦点有关"的交互。

### 变更记录（2026-09-17 晚）
按反馈修"点击按钮进入缩放状态时按钮错位且不隐藏"：
1. **错位**（坑 20）：沉浸态取消面板 transform 过渡 → 控件重新相对**视口**定位。
   实测：修复前距底 50px / 104px（相对图片框），修复后 **30px / 84px / 状态行 132px**（相对视口）。
2. **不隐藏**：新增"点了底部控件就先隐藏一下"（`CHROME_SUPPRESS_MS = 700`）——
   否则指针停在按钮上会被"靠近带"判定为显现，看起来像"点了没隐藏"；指针再动一下就回来。
3. 顺带修两个自身缺陷：状态行固定定位被我上轮清理误删（坑 21）；
   隐藏态的 `translateY(14px)` 让几何断言随显隐漂移（坑 22，改为纯透明度显隐）。
4. 再清理 2 条失效的移动端 `.is-maximized` 覆盖规则。
---

### 变更记录（2026-09-17）
- **沉浸态（最大化 或 已缩放）控件改为"默认隐藏、靠近才显现"**（按需求）：
  - 默认 `opacity: 0` + `pointer-events: none`（隐藏时不挡图、也不误点，可直接在图上拖动平移）
  - `app.js` 的 `initChromeAutoHide()`：指针进入屏幕底部 170px 带内 → 显现（半透明 0.9）；
    离开 → 立即隐藏；键盘缩放（`+ - 0`）→ 临时显现 1.6s，让键盘用户看得到读数
  - `:focus-within` 也显现（Tab 键可达）；`@media (hover: none)` 触屏上常显（0.92）
- **为什么必须用 JS**：隐藏后的元素收不到 hover，纯 CSS 做不到"靠近才出现"；
  旧的"常在半透明 + hover 变实心"还有个副作用（见坑 18）。
- 清理：删掉 2 条被 `is-immersive` 版本完全取代的重复规则
  （`.lightbox.is-maximized .lightbox-panel`、`.lightbox.is-maximized .action-status`）——
  我诊断坑 18 时正是被它们绕进去的；删除后重跑 23/23 断言无回归。

### 变更记录（2026-09-16 深夜）
按反馈修四处问题：
1. **向下/向上拖不到底** 与 **向左拖不动** —— 同一个根因（坑 16）：平移范围按"居中"假设算，
   而特大子项的网格居中会退化成左上对齐。改用 `offsetLeft/offsetTop` 锚点 + `axisRange()`，
   实测现在能一路拖到图片最右缘与最下缘（误差 ≤3px），且中间位置连续可达。
2. **最大化时按钮不淡** —— 透明度 0.3 → **0.22**，并用"停用过渡后再读"的方式复测确认
   （虚拟时间下直接读会拿到过渡起始值，见坑 12/15）。
3. **适应时按钮太靠下** —— 底部三行整体上移（操作条距底 **30px**、缩放条 **84px**、状态行 **132px**），
   并加断言保证"距视口底 ≥20px 且 top ≥0（完整可见）"。
4. 顺带修：**只缩放不最大化**时控件不再被挤出视口（坑 17）。

### 变更记录（2026-09-16 晚）
- **修复"最大化后缩放无变化"**（根因见坑 14）：解除最大化模式对图片尺寸的压制后，
  缩放真正生效——400% 时图片高度已超过视口；滚轮/＋−/键盘/双击在两种模式下都可用。
- **修复"缩放时按钮变形"**：底部三行（档案条 / 缩放条 / 操作条）改为**固定宽度** `min(460px, 86vw)`，
  与图片宽度彻底解耦。实测：面板从 492px 涨到 1354px 时，三行宽度恒为 460px。
- **新增沉浸态（最大化 或 已缩放）**：底部控件与状态行**半透明 0.3**，鼠标移上/键盘聚焦恢复实心；
  空白区域 `pointer-events: none`（按钮本身仍可点），这样能直接在图上拖动平移；
  同时收掉白卡与透明棋盘格，缩放时是"图片浮在暗底上"而不是贴在一张裁切过的白卡里。

### 变更记录（2026-09-16）
- **图片放大升级为「整页查看器 + 真缩放」**（按需求：覆盖首页立绘，不含看板娘/Logo）：
  - 查看器两种来源：`list`（瀑布流点进来，有翻页）与 `single`（页面独立图片，无翻页）；
    首页立绘通过 `#hero-art-window`（`role=button` + 键盘可达）用同一个查看器打开。
  - 真缩放：百分比以**原始像素**为基准（100% = 1:1），下限「适应窗口」、上限 400%；
    滚轮 / 双击 / ＋− 按钮 / 键盘（`+` `-` `0`）都可缩放；双击在「适应 ↔ 100%」间切换。
  - 放大超过可视框后可**按住拖动平移**，并用 `clampPan()` 限制在边界内；
    实测 100% 时渲染宽度与 `naturalWidth` **完全相等**（1055 = 1055）。
  - `Esc` 变为**逐级退出**：先归位缩放 → 再退出最大化 → 最后关闭查看器。
  - 代码落点：`app.js` 的 `openSingleImage` / `applyZoom` / `setZoom` / `clampPan` / `initHeroArtZoom` / `initViewerZoom`；
    `styles.css` 末尾的 `.lightbox-zoom` / `.zoom-button` / `.is-zoomed` / `.is-pannable` 一组规则。

### 变更记录（2026-09-15 晚）
- **为迁移到对象存储/CDN 埋好伏笔**（详见仓库根目录 `架构演进路线图.md`）：
  - `sync_stickers.py`：新增 `STORAGE_MODE`（`local`/`remote`，走环境变量）、`GALLERY_BASE_URL` 等，
    清单结构升级为 `{ storage, items }`；`remote` 模式的 API 契约已写在模块 docstring 里（预埋，未接入）。
  - `app.js`：新增 `getImageUrl(path, kind)` 收口全部取图（缩略图/灯箱/下载/相邻预取），
    并兼容旧的裸数组清单；**每次加载清单都会重置 storage 配置**（否则跨分区切换会残留上一个 CDN 地址——这是实测发现的坑）。
  - `.github/workflows/sync-stickers.yml`：预埋 `vars.GALLERY_*` 环境变量，将来填仓库 Variables 即可切 CDN，无需改代码。
  - `validate-stickers.yml`：校验脚本兼容 `{ storage, items }` 新格式（否则 PR 校验会崩）。
- 清理：`assets/02 .webp`（文件名带空格、实为改名 JPEG、页面未引用）已删除。

### 变更记录（2026-09-15）
- **灯箱「复制图片」已移除**，改为「最大化」查看（`app.js` 的 `setMaximized()` + `styles.css` 的 `.lightbox.is-maximized`）：
  不调用 Fullscreen API，只把白卡装饰收掉、图片放大到 92vh、操作条浮到底部；Esc 先还原再关闭。
  副作用（正向）：不再需要 `fetch()` 原图，**未来图片搬到 CDN 时少一个 CORS 依赖**。
- **瀑布流顺序改为随机**：`renderStickers()` 去重后经 `shuffleInPlace()`（Fisher-Yates）洗牌，
  每次刷新页面 / 切换分区 / 点"重新加载"都重新随机；灯箱翻页顺序自动跟随。

## 七、技巧与可复用做法

- **"线上 vs 仓库"差异对照法**：先搞清楚线上到底在跑哪个版本（比较不存在的旧文件、算 MD5），再谈修。
  本次就是靠"已删除文件仍 200"一眼锁定"旧快照"。
- **部署类问题先要日志**，不要猜。Cloudflare 构建日志一句话就给出了根因。
- **只信魔数不信扩展名**：`file -b`、Pillow `Image.open().format`。
- **占位素材必须一眼可辨**：本次骨架期用渐变块 + "占位图 0N / 请替换为你的素材" 文字，
  避免误当成真实内容上线；同时用 6 种不同宽高比，顺便验证瀑布流。
- **预览设计图用真实素材**：把 `data/` 前 15 张压成小缩略图，用无头 Edge 渲染 1360×1500 的 mockup，
  比"描述风格"有效得多。
- **改动脚本必须回读校验**：本次有一轮"修正断言"的 python 脚本因为**引号语法错误在解析期就失败**，
  什么都写不进去，但输出看起来像"改了"——结果多跑了两轮无效验证才发现。
  凡是脚本改文件，写完立刻 `读回文件` 断言关键片段在位（本项目多次因此受益）。
- **每次发布都做全路径验证**：首页、CSS、JS、清单、`data/<主题>/<分区>/`（含中文名）、`previews/`、`large/`、`logo/`、`memes/`、`assets/`。

---

---

## 八、已知遗留 / 待办

| 优先级 | 事项 | 说明 |
|---|---|---|
| 高 | **来源区与授权协议未按真实来源逐条填写** | 收录他人作品必须填原作者/平台/授权；页脚「非官方整理 · 版权归原作者所有」不可删 |
| 高 | **域名与 ICP 备案未办** | 长期方案（国内 CDN）的唯一硬门槛，1–3 周，不阻塞现有站点；见 `架构演进路线图.md` |
| 中 | whale 主题素材仍是占位 | `data/whale/default/whale-placeholder-01.png`、`data/whale/cos/whale-cos-placeholder-01.png` |
| 中 | 鲸鱼主题文案仍是骨架文案 | `app.js` 的 `themeConfig` 里 whale 的 `copy`，以及 `assets/theme-whale.*` 立绘 |
| 中 | 响应式：窄屏 Hero 主标题溢出 | 用户见过一次，尚未修（改动局限在 `styles.css` 媒体查询） |
| 中 | 看板娘与音效是占位 | `memes/120302wg44ju245iDQ38xu.png`（占位笑脸）、`memes/xixi.aac`（**0.5 秒静音 WAV**，本机无 ffmpeg） |
| 低 | logo 沿用上游文件名 | `logo/deepseek_蓝鲸_*.png`、`assets/deepseek_whale.*`；改名需同步改 `index.html` 引用 |
| 低 | 视觉风格未采纳新方案 | 三版预览图在 `D:\【杂物间】\design-preview\`（用户决定日后再改） |
| 低 | Git 历史仍是 ~162 MB | **刻意不重写历史**（收益暂时用不上、风险高）；等图片迁出 Git 后再评估 |
| 低 | `app.js` 保留上游痕迹 | localStorage 键名 `deepseek-mascot-position`；看板娘资源仍用上游文件名 |

**已完成（不再列为待办）**：品牌文案 7 处占位符 ✅（`82db73a`）｜`_headers` 缓存策略 ✅（`e73f59d`）｜
`assets/02 .webp` 垃圾文件 ✅｜目录结构 `media/` → `data/<主题>/<分区>/` ✅｜
清单升级为 `{ storage, items }` ✅｜查看器缩放 / 平移 / 沉浸态自动隐藏 ✅

---

## 九、上游差异清单（便于日后对照更新）

1. `index.html`：品牌文案已换成真实站名与来源；来源区为「来源 A/B + 授权说明」结构；
   新增查看器所需的结构（缩放控件、立绘可点 + `role=button`、若干 `id` 锚点）。
2. `stickers/manifest_<主题>-<分区>.json`：结构改为 `{ storage, items }`；`alt` 由角色名改为 `DEFAULT_ALT`。
3. `scripts/`：`sync_stickers.py` 改为**自动发现主题/分区**并支持 `STORAGE_MODE`（local/remote）；
   `make_previews.py` 扫描所有分区、产物名带 `主题-分区-` 前缀、新增孤儿产物清理。
4. `styles.css` / `app.js`：**已大幅追加**（查看器缩放·平移·沉浸态、随机顺序、主题书签头、分区切换、
   回顶端、看板娘等）。原则是**追加在文件末尾**，尽量不动上游原有规则 —— 这也让"哪条规则生效"需要留意优先级（见坑 14/20/21）。
5. `_headers`、两个 workflow：已按本项目调整；另新增 `.assetsignore`（部署命门）、`README.md`、
   `UPLOAD.md`、`架构演进路线图.md`、本地工具脚本（`*.bat` / `*.ps1`）与本文件。
6. 上游的立绘 / 表情包 / logo / 看板娘版权素材**已全部清除**，未随本项目分发。

---

## 十、下一步建议

1. **补齐来源与授权**（合规底线）：来源区逐条填原作者/平台/授权协议，并确认页脚声明在位
2. **买域名 + 提交 ICP 备案**：与代码无关、周期 1–3 周，是将来国内 CDN 方案的唯一门槛
3. **修窄屏 Hero 标题溢出**：手机观感问题，改动局限在 `styles.css` 媒体查询
4. 可选：替换 whale 主题的占位素材与文案；按需采纳设计预览图里的视觉方案
5. 图片接近 500 张或 push 明显变慢时，按 `架构演进路线图.md` 把图片迁到对象存储
   （切 CDN 只需在仓库 Variables 填一个域名，不必改代码）

---

## 十一、验收方式约定（避免"验证预算"失控，2026-09-15 定）

> 起因：为修一个"按钮不隐藏"的显示问题，验证设施本身反复卡壳（WSL 单向网络、PowerShell
> 无超时静默挂起、CDP 大消息分片、残留浏览器抢端口、无头下 `:focus-visible` 语义失真），
> 耗时远超改代码本身，且其中大半是你已经说过"不用做"的部分。

1. **默认验收 = 提交推送后，你自己在普通浏览器点一下**（必要时 `Ctrl+Shift+R` 强刷）。
   无头浏览器 / 自动化**不作为常规门禁**。
2. 自动化只允许出现在"没看就等于没做"的地方（例如打包产物、CI 校验脚本），且上限是
   **一张截图**级别的检查；要升级成断言套件时，先问一次"值不值"。
3. **合成事件 ≠ 真实输入**（坑 23）：`dispatchEvent(new MouseEvent('click'))` 不会移动焦点，
   程序化 `focus()` 在无头里还会被判成 `:focus-visible`。凡是 hover / focus / pointer 相关的交互，
   别拿无头断言当门禁 —— 要么真鼠标点，要么别把显隐挂在焦点条件上。
4. **连续两次验证手段失败就止损**：停下，换更粗的手段（截图 / 直接人工确认），
   禁止继续在验证设施上加码。改代码 10 分钟、搭验证 40 分钟，就是越界了。
5. 已知环境事实（省得再查一遍）：
   - WSL 是 NAT：Windows 能访问 WSL 起的服务，**反向访问 Windows 调试端口不通**；
   - 要在 Windows 侧做 CDP：PowerShell + `ClientWebSocket` 可以，但必须自己处理
     **消息分片拼装**、**每步超时**、**残留 Edge 实例抢端口**，`--headless` 下 `:focus-visible` 失真；
   - 本机无 node/playwright（Windows 侧），WSL 侧有 node 24 但没有浏览器的反向往返通路。
