# 开发交接（和武）

> 交接对象：接手本项目的开发者 / 未来的自己
> 项目：把 `EDMOK/blue-fish-archive`（蓝色大肥鱼档案馆）复刻成个人插画收藏站
> 整理时间：2026-09-14
> 编写时状态：**线上可用，自动部署链路已打通并实测通过；品牌文案与视觉风格待定**

---

## 一、成品与地址

| 项 | 值 |
|---|---|
| 线上站点 | https://blue-fish-archive-2.luoyuan2613.workers.dev/ |
| GitHub 仓库 | https://github.com/luoyuan2613-cmyk/blue-fish-archive-2 （Public） |
| 本地目录（WSL） | `/mnt/d/【杂物间】/fish-gallery` |
| 本地目录（Windows） | `D:\【杂物间】\fish-gallery` |
| 设计预览图 | `D:\【杂物间】\design-preview\`（三版方案，尚未采纳） |
| 本地素材备份 | `D:\【杂物间】\local-assets-backup\`（同步仓库时保住的原件） |
| 上游参考项目 | https://github.com/EDMOK/blue-fish-archive |

站点定位：收录**他人创作的插画/动画贺图**（原作版权归各自作者），因此来源区与页脚版权声明不可删。

---

## 二、技术形态（先理解这一层，再动手）

**纯静态站，无框架、无构建、无后端。** 上游的设计与交互全部零修改沿用：

| 职责 | 文件 |
|---|---|
| 页面结构 + 全部文案 | `index.html` |
| 样式（深海贴纸风） | `styles.css`（60KB，零品牌字符串，只有 CSS 变量） |
| 交互 | `app.js`（拉分区清单 → 渲染瀑布流卡片 + 灯箱翻页/最大化/下载） |
| 数据（唯一"数据库"） | `stickers/manifest.json`（由脚本生成，勿手改） |
| 原图 | `media/` ← **日常只操作这个目录** |
| 缩略图 480px | `previews/`（脚本生成） |
| 原尺寸大图 | `large/`（脚本生成，灯箱看图用；「下载原图」仍给原图） |
| 首页立绘 / logo / 看板娘 | `assets/`、`logo/`、`memes/` |
| 生成管线 | `scripts/make_previews.py`、`scripts/sync_stickers.py` |
| CI | `.github/workflows/sync-stickers.yml`、`validate-stickers.yml` |
| 缓存策略 | `_headers`（Cloudflare 约定） |
| 部署忽略清单 | `.assetsignore`（**本次新增，关键文件**） |

**缩略图管线参数**（`make_previews.py`）：`previews/` 480px / WebP q80；`large/` 原尺寸 / WebP q85；
只处理 `png/jpg/jpeg/webp`；**GIF/APNG 跳过**，前端直接播原文件。
`sync_stickers.py` 会把图片真实宽高写进 manifest（取自 preview），前端据此在加载前占位，避免瀑布流整墙抖动。

**主题 / 分区两层结构（2026-09-14 重构）**：

```
data/<主题>/<分区>/   →   stickers/manifest_<主题>-<分区>.json
```

- 主题（`manga` / `whale`）各自拥有**独立的一套分区**（`default` / `cos`），互不混合：
  切主题 → 分区条整体重建为该主题的分区，并加载该主题的清单。
- 目录结构由 `scripts/sync_stickers.py` 的 `discover_partitions()` **自动发现**（有图才生成清单），
  不需要维护分区表；前端在 `app.js` 的 `THEMES` 里补一条同名分区才会显示标签。
- `previews/`、`large/` 的产物名带 **`<主题>-<分区>-` 前缀**。这不是装饰：不同主题常含同名原图
  （两个主题各有一个 `01.jpg`），只按文件名生成会互相覆盖，导致 B 主题显示 A 主题的缩略图。
- 前端清单缓存按 `主题/分区` 分键（`partitionCache`），主题切换不会串数据。

**数据流**：
```
改 media/ → CI 跑两条脚本 → 生成 previews/ + large/ + manifest.json → commit 回仓库 → Cloudflare 自动部署
```

---

## 三、环境与凭据（重要）

- 本机（WSL）**连不上 `github.com:443`**（HTTPS 被阻断），但 **SSH 22 端口可用**、`api.github.com:443` 可用。
  → 所以 git 操作**必须走 SSH**，绝不要用 HTTPS 推送（会像 `git clone` 一样挂死）。
  已验证：`ssh -T git@github.com` → `Hi luoyuan2613-cmyk!`
- 本机**连不上 `*.workers.dev` / `*.pages.dev`**，验证线上站点时用
  `powershell.exe -NoProfile -Command "Invoke-WebRequest ..."`（Windows 侧网络可通）。
- 提交身份：`luoyuan2613-cmyk <luoyuan2613-cmyk@users.noreply.github.com>`
- Python 3.14 + Pillow 12（`pip install Pillow`）
- 预览图渲染链路：Windows 版 Edge 无头模式（本机无 node/playwright）：
  ```bash
  EDGE="/mnt/c/Program Files (x86)/Microsoft/Edge/Application/msedge.exe"
  "$EDGE" --headless=new --disable-gpu --hide-scrollbars --force-device-scale-factor=1 \
    --window-size=1360,1500 --screenshot="C:\Users\Public\dsh-tmp\out.png" \
    "file:///C:/Users/Public/dsh-tmp/mk/vA.html"
  ```
  注意：无头 Edge **走系统代理，连不上 workers.dev**；要渲染线上页面，必须先把线上文件镜像到本地再用 `file://` 渲染。
- ⚠️ **不要把任何 token / API Key 写进本文件或仓库**。

---

## 四、常用操作

### 加图（日常）
方式一（网页，零安装）：`https://github.com/luoyuan2613-cmyk/blue-fish-archive-2/tree/main/media`
→ `Add file` → `Upload files` → Commit → 等 1~2 分钟自动上线。

方式二（本地批量）：
```bash
cd "/mnt/d/【杂物间】/fish-gallery"
python scripts/make_previews.py
python scripts/sync_stickers.py
git add -A && git commit -m "add images" && git push
```

### 删图
`media/`、`previews/`、`large/`、`manifest.json` 四处必须一起处理：
```bash
rm media/xxx.jpg previews/xxx.webp large/xxx.webp
python scripts/sync_stickers.py
```
或者删完去 Actions 页面手动 `Run workflow`（见坑 1）。

### 本地预览
```bash
cd "/mnt/d/【杂物间】/fish-gallery" && python3 -m http.server 8000
# 浏览器打开 http://127.0.0.1:8000/
```

---

## 五、踩过的坑（按重要性排序，全部已实测）

### 坑 1（致命）：Cloudflare 自动部署从未成功 —— 静态资源超 25 MiB
**现象**：线上站点永远停在很久以前的快照：已被删除的 `assets/deepseek_whale.png` 仍返回 200，新上传的 `assets/001.png` 一直 404，但 HTML 文案却是最新的。手动部署能成功，自动构建全红叉。

**定位过程**（可复用的排查套路）：
1. 对比"线上能访问的文件"与"仓库里存在的文件"——发现线上存在的是**仓库里早已删除的文件**，说明线上是旧快照。
2. 取线上文件字节算 MD5，与骨架期提交里的占位图对比 → **MD5 完全一致**，确认快照时间点。
3. 向用户要 Cloudflare 构建日志 → 一眼看到：
   ```
   X [ERROR] Asset too large.
   Cloudflare Workers supports assets with sizes of up to 25 MiB.
   We found a file /opt/buildhome/repo/.git/objects/pack/pack-3fe6e46...
   Ensure all assets in your assets directory "/opt/buildhome/repo" conform ...
   ```
4. 仓库里没有任何 `wrangler.toml/jsonc`，Cloudflare 构建设置是：构建命令 `无`、部署命令 `npx wrangler deploy`、根目录 `/`，
   构建时由 wrangler 交互式生成配置，`"assets": { "directory": "." }` —— **整个仓库根目录当静态资源**，于是 `.git/` 里的 pack 对象（>25 MiB）被算进去。

**修复**：仓库根目录新增 `.assetsignore`（Wrangler 的标准忽略文件，与 `.gitignore` 语法相同）：
```
.git
.github
.assetsignore
*.md
scripts
```
**验证**：推送后 45 秒，`assets/001.png` 200、`assets/001.webp` 200、`deepseek_whale.png` 404（幽灵文件消失），
并逐个验证 `media/`（含中文名）、`previews/`（含中文名）、`large/`、`logo/`、`memes/`、`stickers/manifest.json` 全部 200。

**经验**：`npx wrangler deploy` + `assets.directory = "."` 的组合**必须**配 `.assetsignore` 排除 `.git`，
否则 Git 仓库一大就必然部署失败，而且报错出现在最后一步，很容易误判成"网络/权限"问题。

### 坑 2：工作流的路径触发范围比想象中窄
`sync-stickers.yml` 只在 `media/**`、`scripts/*.py`、它自己 yml 变化时触发。
→ **单独推 `stickers/manifest.json` 或只删 `previews/`、`large/` 里的文件不会触发任何 CI**。
→ 补救：Actions 页面手动 `Run workflow`（`workflow_dispatch` 已启用）。
→ 安全做法：永远通过改 `media/` 增删图，本地跑完两条脚本后**一起**提交。
（机器人自己的提交只改 `previews/`、`large/`、`manifest.json`，所以不会自我触发，没有死循环风险。）

### 坑 3：HTTP 缓存把 404 "焊死"在浏览器里【已修复】
`_headers` 里 `/assets/*` 等原本是 `public, max-age=604800`（7 天）。
修复部署前浏览器请求立绘拿到 404 并缓存，**修复后仍显示空白**，必须强刷（`Ctrl+Shift+R`）或无痕窗口。
自测技巧：加 `?v=1` 之类的参数绕开缓存。

**已修复（提交 `e73f59d`）**：`/assets/*`、`/logo/*`、`/memes/*` 的 `max-age` 由 604800 改为 **600（10 分钟）**，
理由与后果都写进了 `_headers` 的注释。`media/`、`previews/`、`large/` 保持 `immutable` 不变。
线上实测（逐个核对响应头）：
```
/index.html /styles.css /app.js /stickers/manifest.json → public, max-age=0, must-revalidate
/assets/* /logo/* /memes/*                              → public, max-age=600
/media/* /previews/* /large/*                           → public, max-age=31536000, immutable
```
→ 仍然需要**手工强刷一次**：已经被旧策略缓存到访客本地的 404/旧图不会自动失效，`max-age` 只影响之后的请求。

### 坑 3.5：部署切换期站点会短暂不可达
推送后 Cloudflare 替换实例的几十秒里，`workers.dev` 域名的请求会**全部超时**（本机 `Invoke-WebRequest` 报"无法连接到远程服务器"），
而同一时刻百度、cloudflare.com 都正常 —— 别误判成"站点挂了"或"网络坏了"，等 1 分钟再试即可。
实测：改 `_headers` 后约 60 秒恢复，恢复时新缓存头已生效。

### 坑 4：Cloudflare 项目名与域名形态
用户实际拿到的是 **Workers 项目**，域名是 `blue-fish-archive-2.luoyuan2613.workers.dev`，
**不是** `blue-fish-archive-2.pages.dev`（Pages 域名探测一直失败，别浪费时间）。
`_headers` 在 Workers 静态资源下同样生效（实测响应头里有 `Cache-Control: public, max-age=0, must-revalidate`）。

### 坑 5：PowerShell 抓网页的编码陷阱
`Invoke-WebRequest | Out-File -Encoding utf8` 会加 BOM 且把 UTF-8 当 Latin-1 读，导致中文全变乱码、
逐字节 diff 出 131 行假差异。**正确做法**：把原始字节写盘再比对：
```powershell
[System.IO.File]::WriteAllBytes($out, $r.RawContentStream.ToArray())
```
另：`curl.exe` 是 Windows 程序，**写不了 WSL 的 `/tmp`**，输出路径要给 Windows 路径。

### 坑 6：文件名里的空格与"假 WebP"
`assets/02 .webp`（`02` 与扩展名之间有空格）这类名字在 URL 里要编码成 `%20`，极易踩坑；
且那个文件和同名 `.jpg` **字节数完全相同（194388）**——它只是改了扩展名的 JPEG，不是真 WebP。
→ 用户已自行删除。提交前建议统一用 ASCII 无空格文件名（图片本身放 `media/` 时中文名是没问题的）。
→ 判断只信魔数，别信扩展名：`file -b x.webp` 会打印 `RIFF ... Web/P image` 才是真 WebP。

### 坑 7：本地仓库与远端分叉
用户直接在 GitHub 网页上编辑（`Update index.html`、`Add files via upload`），本地仓库会落后十几个提交；
若本地还有未提交的删除/新增，`git merge --ff-only` 会被拒（"Please move or remove them before you merge"）。
处理顺序：**先备份未跟踪文件到仓库外 → 丢弃冗余改动 → 同步**。本次备份在 `D:\【杂物间】\local-assets-backup\`。

### 坑 8：CI 回写权限要显式开启，且"空跑"验证不了
`Settings → Actions → General → Workflow permissions` 必须是 **Read and write**，
否则机器人无法把生成的缩略图/清单推回仓库（Web UI 无法用 API 查，未授权返回 401）。
**验证技巧**：故意制造"必须回写"的场景——往 `media/` 放一张新图但**不**本地生成缩略图，
推送后 CI 必须生成并 commit。实测成功后提交历史里会出现 `github-actions[bot] chore: sync sticker manifest and previews`。

### 坑 9：上游把 `alt` 硬编码成角色名
`sync_stickers.py` 里原本写死 `"alt": "鲸鱼娘同人表情包"`。
本项目已提取为常量，改这一行即可：
```python
DEFAULT_ALT = "动画贺图收藏"
```

---

### 坑 10：跨主题同名文件导致缩略图互相覆盖
`previews/`、`large/` 早期只按 `path.stem` 命名（`01.webp`）。主题化之后，两个主题各有一个 `01.jpg`，
必然覆盖 → A 主题显示 B 主题的图。**解决**：产物名带 `<主题>-<分区>-` 前缀。
**教训**：任何"按文件名生成中间产物"的管线，在引入第二套同名数据源时都会静默出错，
且页面不报错、只是图不对——比 404 更难发现。

### 坑 11：初始化顺序错误被"后来的功能"掩盖
`initTheme()` 里原本是 `applyTheme()` 先、`initThemeTabs()` 后。`applyTheme` 内部会遍历
`.theme-tab` 标记选中态，而那时标签还没建出来 → **页面主题正确、但没有任何书签头是高亮状态**。
上一版把分区逻辑排在主题之后，恰好掩盖了它；这版重构才暴露。
**教训**：凡是"A 函数操作 B 函数创建的 DOM"，初始化顺序必须显式写清并加注释；
这类 bug 不会抛错，只会"看起来正常"。

### 坑 12：无头浏览器验证的三个陷阱（会让人误判代码有 bug）
用 Edge `--headless=new` + `--virtual-time-budget` 做自动化验收时，以下三件事**在虚拟时间下不工作**，
表现为"代码明明对、断言却失败"：

| 陷阱 | 现象 | 对策 |
|---|---|---|
| `window.scrollTo()` | 调用后 `scrollY` 恒为 0 | 改用 `document.scrollingElement.scrollTop = N` 制造滚动状态；并用 spy 替换 `scrollTo` 验证调用 |
| `html { scroll-behavior: smooth }` | `scrollTop` 赋值变成动画，读到中间值（实测 419） | 测试期间临时 `documentElement.style.scrollBehavior = 'auto'` |
| CSS `transition` | 类生效后 `computed opacity` 仍停在 0 | 测试期间临时 `el.style.transition = 'none'` 再读计算样式 |

另：**Edge 命令会被转交给已存在的实例**，此时 `--user-data-dir` / `--screenshot` 会被忽略（表现为截图内容不变、
进程数不增加）。每次验证都用一个新的 profile 目录，并确认没有并发调用。

### 坑 13：删图不会清理旧产物（已修）
`make_previews.py` 原先只生成、不删除，所以删掉一张图后 `previews/`、`large/` 里的旧文件会永久残留
（还会被 CI 一起提交、一起部署）。现已加入孤儿清理（默认开启，`--no-prune` 可关），
只清理"前缀仍被发现"的分区，避免因为目录暂时缺失误删整批产物。

### 变更记录（2026-09-15）
- **灯箱「复制图片」已移除**，改为「最大化」查看（`app.js` 的 `setMaximized()` + `styles.css` 的 `.lightbox.is-maximized`）：
  不调用 Fullscreen API，只把白卡装饰收掉、图片放大到 92vh、操作条浮到底部；Esc 先还原再关闭。
  副作用（正向）：不再需要 `fetch()` 原图，**未来图片搬到 CDN 时少一个 CORS 依赖**。
- **瀑布流顺序改为随机**：`renderStickers()` 去重后经 `shuffleInPlace()`（Fisher-Yates）洗牌，
  每次刷新页面 / 切换分区 / 点"重新加载"都重新随机；灯箱翻页顺序自动跟随。

## 六、技巧与可复用做法

- **"线上 vs 仓库"差异对照法**：先搞清楚线上到底在跑哪个版本（比较不存在的旧文件、算 MD5），再谈修。
  本次就是靠"已删除文件仍 200"一眼锁定"旧快照"。
- **部署类问题先要日志**，不要猜。Cloudflare 构建日志一句话就给出了根因。
- **只信魔数不信扩展名**：`file -b`、Pillow `Image.open().format`。
- **占位素材必须一眼可辨**：本次骨架期用渐变块 + "占位图 0N / 请替换为你的素材" 文字，
  避免误当成真实内容上线；同时用 6 种不同宽高比，顺便验证瀑布流。
- **预览设计图用真实素材**：把 `media/` 前 15 张压成小缩略图，用无头 Edge 渲染 1360×1500 的 mockup，
  比"描述风格"有效得多。
- **每次发布都做全路径验证**：首页、CSS、JS、manifest、`media/`（含中文名）、`previews/`、`large/`、`logo/`、`memes/`、`assets/`。

---

## 七、已知遗留 / 待办

| 优先级 | 事项 | 说明 |
|---|---|---|
| 中 | whale 主题是占位素材 | `data/whale/default/whale-placeholder-01.png`、`data/whale/cos/whale-cos-placeholder-01.png` 待替换 |
| 高 | 品牌文案占位符未填 | `index.html` 中 7 处 `【】`（`grep -n "【" index.html`），含站名、副标题、主标题、一句话简介、来源区、页脚署名、社交链接（目前 `href="#"`） |
| 高 | 响应式问题 | 窄屏下 Hero 主标题文案溢出，用户已见过一次 |
| 中 | ~~`_headers` 缓存策略~~ | ✅ 已完成（`e73f59d`）：品牌素材 7 天 → 10 分钟，见坑 3 |
| 中 | 来源区尚未如实填写 | 收录他人作品，必须填原作者/平台/授权协议；页脚"非官方整理 · 版权归原作者所有"不可删 |
| 低 | 视觉风格 | 上游"深海贴纸风"与暖色插画调性不搭，三版预览图已出（暖纸感 / 暗场展陈），用户决定日后再改 |
| 低 | 看板娘与音效是占位 | `memes/120302wg44ju245iDQ38xu.png`（占位笑脸）、`memes/xixi.aac`（**0.5 秒静音 WAV，因无 ffmpeg 无法编码 AAC**） |
| 低 | logo 沿用上游文件名 | `logo/deepseek_蓝鲸_彩色.png`、`deepseek_蓝鲸_黑色.png`、`assets/deepseek_whale.png` 是占位图，**改名要同步改 `index.html` 引用** |
| 低 | `app.js` 保留上游痕迹 | localStorage 键名 `deepseek-mascot-position`；按需求刻意保持与上游一致，未改 |

---

## 八、上游差异清单（便于日后对照更新）

1. `index.html`：品牌文案占位化；来源区改为通用"来源 A/B + 授权协议"结构；社交与来源链接改为占位 `href`；新增 `<!-- 【替换点 N】 -->` 注释。
2. `stickers/manifest.json`：`alt` 由角色名改为 `DEFAULT_ALT`。
3. `scripts/sync_stickers.py`：新增 `DEFAULT_ALT` 常量（唯一代码改动）。
4. `styles.css`、`app.js`、两个 workflow、`_headers`：**与上游完全一致**（除新增 `.assetsignore`、`README.md`、`UPLOAD.md`、本文件）。
5. 上游的立绘/表情包/logo/看板娘版权素材**已全部清除**，未随本项目分发。

---

## 九、交接后的第一条建议

先把**品牌文案 7 处**填完（`grep -n "【" index.html` 逐个替换），再做**窄屏 Hero 溢出**修复，
然后按需决定是否采纳设计预览图里的方案 A 或 B。这三件做完，站点就从"能跑"变成"能见人"。
