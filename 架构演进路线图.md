# 架构演进路线图

> 面向对象：站主本人（非专业开发）与未来接手的人
> 编写时间：2026-09-15
> 数字校订：2026-09-15（§1 的实测表已按当日实际重刷）
> 配套代码：本次已完成"伏笔改造"（见 §3），**当前线上行为与改造前完全一致**

---

## 0. 一句话结论

**现在（453 张 / 108 MB，仍在 500 张以内）不需要搬家，但伏笔已经埋好**：图片与代码都留在 Git 里照常工作；
等图片变多、或国内访问实在难忍时，按 §4 的清单**只改配置、不改代码**即可迁移到对象存储 + 国内 CDN。

---

## 1. 现状与瓶颈（实测数据）

| 项目 | 实测（2026-09-15 重刷） | 说明 |
|---|---|---|
| 源图 | **453 张 / 108 MB** | `data/<主题>/<分区>/` |
| 缩略图 | 452 张 / 12 MB | `previews/`，瀑布流用（1 张 `.gif` 动图不产 webp） |
| 灯箱大图 | 452 张 / 44 MB | `large/`，看图用 |
| `.git` 仓库 | **204 MB** | 每次 push 都要协商这些历史对象 |
| 最大单张 | 5.2 MB（`data/manga/cos/高清观看版.jpg`） | 距 Cloudflare 单文件 25 MiB 上限还很远 |

**两个痛点的根因**：

1. **推送慢 / 易断**：网页主体只有几百 KB，慢的是**上百 MB 的图片历史**在慢速 SSH 上传输。
2. **国内访问不稳**：`*.workers.dev` 在大陆时通时不通；即便换成 Cloudflare 自定义域名，
   国内链路质量也不受控。图片如果继续放在 Cloudflare 上，**同样不稳**。

---

## 2. 目标架构（图片与网页分流）

```
                    ┌──────────────────────────────┐
   国内访客 ───────▶ │ 国内 CDN（七牛 / 阿里云 OSS） │──── 图片（原图 + 缩略图）
                    └──────────────────────────────┘
                                ▲
                                │ 上传（CI 自动 / 控制台手动）
                                │
   Git 仓库（只留代码 + 清单 + 缩略图 + 小图）──▶ 对象存储桶
                                │
                    ┌──────────────────────────────┐
   访客（含国外）──▶ │ Cloudflare Workers（网页主体）│──── index.html / app.js / 清单
                    └──────────────────────────────┘
```

要点：
- **网页**（HTML/CSS/JS/清单）继续走 Cloudflare —— 体积小、更新频繁、无需备案。
- **图片**走国内对象存储 + CDN —— 体积大、更新少、可长期缓存。
- 两者通过清单里的 `storage.baseUrl` 关联（本次已实现）。

---

## 3. 已经埋好的"伏笔"（本次改动，均已实测）

| 伏笔 | 位置 | 怎么用 |
|---|---|---|
| 清单新增 `storage` 段 | `scripts/sync_stickers.py` | 清单从裸数组变成 `{ storage, items }`；`baseUrl` 为空＝相对路径＝**和以前一样** |
| 前端地址收口 | `app.js` 的 `getImageUrl(path, kind)` | 所有取图（缩略图/灯箱/下载/相邻预取）都经过它；换 CDN **只改清单里的 baseUrl** |
| 存储模式开关 | `GALLERY_STORAGE_MODE`（`local` / `remote`） | `remote` 时不再扫本地目录，改从 API 拉文件列表（接口已预埋，尚未接入真实服务） |
| CI 预埋变量 | `.github/workflows/sync-stickers.yml` | 在 GitHub 仓库 Variables 里填 `GALLERY_BASE_URL` 等，CI 生成的清单就带上 CDN 地址 |
| 兼容旧数据 | `app.js` `fetchPartition()` + CI 校验脚本 | 旧的裸数组清单**照样能读**，不会因为格式升级而打不开 |

**remote 模式的接口约定**（未来对接时照这个实现即可，请求会带 `?theme=<主题>&partition=<分区>`）：

```json
{
  "storage": { "baseUrl": "https://img.example.com/", "previewBaseUrl": "", "largeBaseUrl": "" },
  "files": [
    { "path": "data/manga/default/01.jpg", "filename": "01.jpg",
      "preview": "previews/manga-default-01.webp",
      "large":   "large/manga-default-01.webp",
      "width": 1200, "height": 1600, "alt": "" }
  ]
}
```

---

## 4. 三阶段路线图

### 近期（现在～500 张）：只做准备，不动架构
- [x] 完成上面的"伏笔改造"（已完成）
- [ ] **买域名 + 提交 ICP 备案**（这是长期方案唯一的硬门槛，周期 1–3 周，越早越好）
- [ ] 定期清理无用图片（仓库里每多一张，每次 push 都慢一点）
- 原则：**不重写 Git 历史**（风险高、收益暂时用不上）

> **当前进度（2026-09-15）**：453 张 / 108 MB —— 张数已到第一条触发线的 **91%**，
> 也就是说**再收录约 50 张就该按 §6 的中期流程动手了**（`data/` 体积离 500 MB 还很远）。

**触发迁移的信号**（满足任意两条就该进中期）：
- 源图超过 ~500 张，或 `data/` 超过 500 MB
- `git push` 经常超过 2 分钟或频繁断连重传
- 想给朋友/手机看，但图片加载经常转圈

### 中期（500～2000 张）：图片搬离 Git，上传走 CI
1. 选一家对象存储（见 §5），建桶、开启静态访问（或用 CDN 域名）
2. 桶上生成一对**仅能写这个桶**的密钥（AccessKey/SecretKey）
3. 把密钥存进 **GitHub 仓库 Secrets**（绝不进代码、绝不进清单）
4. 加一个 GitHub Actions 工作流：把 `data/` 里新增的图 `rclone`/`ossutil`/`coscli` 同步到桶里
5. 在仓库 **Variables** 里填 `GALLERY_BASE_URL`（CDN 域名）→ CI 生成的清单自动指向 CDN
6. 确认线上图片正常后，把 `data/` 从 Git 里移除（`git rm -r --cached data`，本地文件保留），
   **Git 只留代码 + 清单 + `previews/`**
7. 可选：灯箱大图 `large/` 也一起搬走（省 35 MB）

**收益**：仓库缩到几 MB，push 秒级；图片走国内线路，访问稳定。

### 长期（>2000 张）：国内 CDN + 一致性
- 图片全部走**已备案的自定义域名 + 国内 CDN**（七牛/阿里云/腾讯云）
- `previews/` 也上 CDN（瀑布流首屏更快）
- 网页主体若在国内仍不稳：再加一个国内镜像域名（同一份静态文件，换一台国内主机/OSS 静态托管）
- 视需要重写 Git 历史瘦身（`git filter-repo`），或干脆启用新仓库重新开始
- 视需要把"上传"做成网页后台：Cloudflare Worker 持有密钥，签发**临时上传凭证**给浏览器直传

---

## 5. 选型对比（2026-09 的价格与可达性）

| 方案 | 国内可达性 | 备案要求 | 成本（个人量级） | 备注 |
|---|---|---|---|---|
| **七牛云 Kodo** | 好（有国内 CDN） | CDN 域名需备案 | **每月 10 GB 存储 + 10 GB 回源流量免费**，超出约 ¥0.1–0.3/GB | 对个人图站最友好，推荐首选 |
| **阿里云 OSS** | 好 | 自定义域名/CDN 需备案 | 存储 **¥0.12/GB/月** 起；**上传流量免费**；外网流出流量另计 | 生态最全，文档最全 |
| **腾讯云 COS** | 好 | 同上 | 与 OSS 接近 | 若已有腾讯云账号可选 |
| **Cloudflare R2** | **差**：`r2.dev` 公共域名在大陆被大面积阻断 | 无需备案 | 存储便宜、**出流量免费** | 适合"只给国外看"或配合自己的域名；**不能解决国内访问** |

> 成本直觉：你现在 108 MB 源图 + 56 MB 产物 ≈ **0.16 GB**（2026-09-15 实测），
> **七牛免费额度完全覆盖**；即使涨到 2000 张（约 1 GB）+ 每月 20 GB 流量，也大约 ¥3–10/月。

---

## 6. 中期迁移操作手册（未来照做；含回滚）

```bash
# ① 备份（重要）
cp -r data /path/to/backup/data-$(date +%F)

# ② 上传到对象存储（示例：rclone，配置里用只读/只写该桶的密钥）
rclone sync data remote:bucket-name/data --progress

# ③ 让清单指向 CDN（本地验证用）
GALLERY_BASE_URL="https://img.example.com/" python scripts/sync_stickers.py
python -m http.server 8000     # 打开页面确认图片全部来自 CDN

# ④ 通过后提交（此时 data/ 仍在 Git 里，双轨运行一段时间最安全）

# ⑤ 稳定 1–2 周后，把图片移出 Git（本地文件保留）
git rm -r --cached data
echo "data/" >> .gitignore      # 只忽略 Git，不影响本地文件与 CI 上传
git commit -m "chore: 图片迁出 Git，改由对象存储/CDN 提供"
```

**回滚**：把 `GALLERY_BASE_URL` 清空 → 重新生成清单 → 页面即回到相对路径（图片文件一直在本地与桶里）。

**注意**：
- `.gitignore` 里加了 `data/` 之后，**CI 就看不到新图了** —— 中期要把"上传到桶"的工作流改成从本地上传，
  或保留一个小体积的 `data/` 索引；这一步建议到时候单独设计。
- **跨域下载**：浏览器会忽略跨域链接的 `download` 文件名属性，迁移后"下载原图"可能直接在新标签打开，
  或按远端文件名保存。属已知取舍。

---

## 7. 密钥与安全原则（务必遵守）

1. **密钥永远不进代码、不进清单、不进文档**（本仓库已按此实现：脚本只读环境变量）。
2. 只放三处之一：**GitHub Actions Secrets**（CI 上传用）、**Cloudflare Worker 环境变量**（签发临时凭证用）、
   或你自己的服务器环境变量。
3. 密钥权限最小化：只给单个桶的读写，别用主账号的全局密钥。
4. 桶上开启 **Referer 防盗链**（只允许你的域名），防止别人白嫖流量产生费用。
5. 定期在服务商后台看账单与流量图，异常立刻换密钥。

---

## 8. 风险与注意事项

| 风险 | 说明 | 缓解 |
|---|---|---|
| 备案周期 | 国内 CDN/自定义域名必须备案，1–3 周 | **现在就买域名备案**，与代码无关，不阻塞 |
| Git 历史瘦身 | `filter-repo` 会改写所有提交哈希，需要所有人重新 clone | 放到长期阶段；中期只用 `git rm --cached` 停止增长 |
| 缓存一致性 | CDN 会长期缓存，换图必须改文件名 | 已有约定：换图改文件名；`data/` 是 immutable |
| 双轨期数据不一致 | 本地有新图但没上传到桶 | CI 上传工作流 + 上传后再改清单 |
| 免费额度被刷 | 图片被外站盗链 | 开防盗链、监控流量 |
| 域名被墙/污染 | 任何单一域名都有风险 | 网页与图片分开域名，出问题只影响一半 |

---

## 9. 待办清单

**你现在就能做（不依赖我、不依赖代码）**
- [ ] 买一个域名（阿里云/腾讯云注册，方便后续备案）
- [ ] 提交 ICP 备案（个人性质，身份证 + 网站信息；备案期间不影响现有站点）
- [ ] 决定是否清理存量图片（`assets/02 .webp` 已在本次清理；可在 `data/` 里挑重复/低质图删掉）

**达到中期触发条件时再做**
- [ ] 选存储服务商（推荐先试七牛免费额度）
- [ ] 建桶 + 开 CDN 域名 + 开防盗链
- [ ] 建只写该桶的密钥，存进 GitHub Secrets
- [ ] 加"CI 上传图片到桶"的工作流
- [ ] 填仓库 Variables：`GALLERY_BASE_URL`
- [ ] 线上验证图片全部来自 CDN（F12 → Network 看域名）
- [ ] 稳定后再 `git rm --cached data`

**长期**
- [ ] `large/`、`previews/` 也上 CDN
- [ ] 评估国内镜像域名（若网页主体在国内仍不稳）
- [ ] 评估"网页后台上传"（Worker 签发临时凭证）
- [ ] 一次性 Git 历史瘦身或迁新仓库
